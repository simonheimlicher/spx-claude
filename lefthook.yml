pre-commit:
  commands:
    prettier:
      glob: "*.{md,json}"
      run: prettier --write {staged_files}
    markdownlintcli2:
      glob: "*.{md}"
      run: markdownlint-cli2 --config ~/.markdownlint.yaml --fix {staged_files}
